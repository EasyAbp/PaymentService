version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=Prepayment_Identity
      - Prepayment_DB=Prepayment_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
